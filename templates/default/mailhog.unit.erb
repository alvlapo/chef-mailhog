[Unit]
Description=MailHog Email Catcher
After=syslog.target network.target

[Service]
Type=simple
ExecStart=<%= @binary %> <% @opts.each do |k,v| %><%= "-#{k} #{v}" %> <% end %>
StandardOutput=journal
Restart=on-failure

[Install]
WantedBy=multi-user.target
